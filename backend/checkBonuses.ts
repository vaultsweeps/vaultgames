import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const bonuses = await prisma.bonus.findMany(); console.log(bonuses); } main().finally(() => prisma.$disconnect());
